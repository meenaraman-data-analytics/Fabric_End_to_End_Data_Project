# Scripts

JSON metadata files where the input values for the raw data source connections and its destinations are parameterized so that a lookup activity in the pipeline can can loop through and load the raw data in the defined location without having to copy or upload each file manually.
